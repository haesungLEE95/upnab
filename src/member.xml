<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memberns">
	<resultMap type="member" 		id="memberResult">
		<result property="id" 		column="id" />
		<result property="password" column="password" />
		<result property="name" 	column="name" />
		<result property="address" 	column="address" />
		<result property="tel" 		column="tel" />
		<result property="reg_date" column="reg_date" />
		<result property="update_date" column="update_date" />
		<result property="del" 		column="del" />
	</resultMap>
	<select id="select" parameterType="string" resultType="member">
		select * from member2 where id=#{id}
	</select>
	<insert id="insert" parameterType="member">
		insert into member2 values (#{id}, #{password}, #{name},
			#{address}, #{tel}, sysdate, sysdate, 'n')
	</insert>
	<update id="update" parameterType="member">
		update member2 set password=#{password}, name=#{name},
		 	tel=#{tel}, address=#{address},update_date=sysdate	 	
		 	where id=#{id}
	</update>
	<update id="delete" parameterType="string">
		update member2 set del='y' where id=#{id}	
	</update>	
</mapper>